services:
  - type: web
    name: pitch-iq-soccer-predictor
    runtime: python
    plan: free
    buildCommand: python -m pip install -r requirements.prod.txt
    startCommand: python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
